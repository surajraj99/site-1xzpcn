{
  "name": "anniversary",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\"",
    "serve": "python3 -m http.server 8080 --directory site"
  }
}
